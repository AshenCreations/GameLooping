{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run",
            "program": "${workspaceFolder}/someThing.exe",
            "request": "launch",
            "type": "cppvsdbg",
            "cwd": "${workspaceFolder}"
        }
    ]
}