{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run",
            "program": "${workspaceFolder}/GameLooping.exe",
            "request": "launch",
            "type": "cppvsdbg",
            "cwd": "${workspaceFolder}"
        }
    ]
}